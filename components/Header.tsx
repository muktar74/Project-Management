import React from 'react';

// This component is not currently used in the application.
// It has been given a minimal implementation to resolve a module parsing error
// that was caused by this file being empty.
const Header: React.FC = () => {
  return null;
};

export default Header;
